apiVersion: apps/v1
kind: Deployment
metadata:
  name: carousels
  labels:
    app: carousels
spec:
  replicas: 2
  # strategy:
  #   type: RollingUpdate
  #   rollingUpdate:
  #     maxSurge: 2        # how many pods we can add at a time
  #     maxUnavailable: 0
  selector:
    matchLabels:
      app: carousels
  template:
    metadata:
      labels:
        app: carousels
    spec:
      containers:
      - name: carousels
        image: soberservicesguy/portfolio-images:ecommerce_carousels_4
        resources:
          limits:
            cpu: "200m"
            memory: "300Mi"
        ports:
        - containerPort: 5006
        env:
        - name: MONGODB_USER
          valueFrom:
            secretKeyRef:
              name: ecommerce-mongodb-secret
              key: MONGO_ROOT_USERNAME
        - name: MONGODB_PASS
          valueFrom:
            secretKeyRef:
              name: ecommerce-mongodb-secret
              key: MONGO_ROOT_PASSWORD
        - name: MONGODB_SERVER
          valueFrom:
            configMapKeyRef:
              name: ecommerce-mongodb-configmap
              key: MONGO_SERVER
      imagePullSecrets:
      - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: carousel-service
spec:
  type: NodePort
  # type: ClusterIP
  selector:
    app: carousels
  ports:
    - protocol: TCP
      targetPort: 5006
      port: 5006