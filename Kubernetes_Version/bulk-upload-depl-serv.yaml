apiVersion: apps/v1
kind: Deployment
metadata:
  name: bulk-upload
  labels:
    app: bulk-upload
spec:
  replicas: 2
  # strategy:
  #   type: RollingUpdate
  #   rollingUpdate:
  #     maxSurge: 2        # how many pods we can add at a time
  #     maxUnavailable: 0
  selector:
    matchLabels:
      app: bulk-upload
  template:
    metadata:
      labels:
        app: bulk-upload
    spec:
      containers:
      - name: bulk-upload
        image: soberservicesguy/portfolio-images:ecommerce_bulk_upload_19
        resources:
          limits:
            cpu: "200m"
            memory: "300Mi"
        ports:
        - containerPort: 5002
        env:
        - name: MONGODB_USER
          valueFrom:
            secretKeyRef:
              name: ecommerce-mongodb-secret
              key: MONGO_ROOT_USERNAME
        - name: MONGODB_PASS
          valueFrom:
            secretKeyRef:
              name: ecommerce-mongodb-secret
              key: MONGO_ROOT_PASSWORD
        - name: MONGODB_SERVER
          valueFrom:
            configMapKeyRef:
              name: ecommerce-mongodb-configmap
              key: MONGO_SERVER
      imagePullSecrets:
      - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: bulk-upload-service
spec:
  type: NodePort
  # type: ClusterIP
  selector:
    app: bulk-upload
  ports:
    - protocol: TCP
      targetPort: 5002
      port: 5002